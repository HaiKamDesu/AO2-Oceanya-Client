<local:OceanyaWindowContentControl x:Class="OceanyaClient.CallwordRuleEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OceanyaClient"
        Height="330"
        Width="620"
        MinHeight="300"
        MinWidth="560">
    <local:OceanyaWindowContentControl.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF262626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="MinHeight" Value="30"/>
        </Style>
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FF1E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="MinHeight" Value="30"/>
        </Style>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFE2E2E2"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="HelpGlyph" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF9FC3DF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Text" Value="(?)"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </local:OceanyaWindowContentControl.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock Text="Callword" Style="{StaticResource LabelText}"/>
            <TextBlock Style="{StaticResource HelpGlyph}" ToolTip="Word or phrase to detect in incoming IC/OOC messages. Match is case-insensitive."/>
        </StackPanel>
        <TextBox Grid.Row="1" x:Name="CallwordTextBox" Style="{StaticResource DarkTextBox}" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock Text="Sound" Style="{StaticResource LabelText}"/>
            <TextBlock Style="{StaticResource HelpGlyph}" ToolTip="Optional custom AO2-compatible sound. Empty uses default word_call notification SFX."/>
        </StackPanel>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SoundPathTextBox" Style="{StaticResource DarkTextBox}" IsReadOnly="True" ToolTip="Selected custom SFX file. Use Browse; do not type paths."/>
            <local:PlayStopProgressButton Grid.Column="1"
                                          x:Name="PreviewButton"
                                          Width="36"
                                          Height="36"
                                          Margin="8,0,8,0"
                                          ProgressEnabled="True"
                                          AutoProgress="True"
                                          ToolTipText="Preview selected or default callword SFX."
                                          PlayRequested="PreviewButton_PlayRequested"
                                          StopRequested="PreviewButton_StopRequested"
                                          PlaybackCompleted="PreviewButton_PlaybackCompleted"/>
            <Button Grid.Column="2" Content="Browse" Style="{StaticResource ModernButton}" Margin="0,0,8,0" Click="BrowseButton_Click" ToolTip="Pick an AO2-compatible audio file."/>
            <Button Grid.Column="3" Content="Use Default" Style="{StaticResource ModernButton}" Click="UseDefaultButton_Click" ToolTip="Clear custom sound and use AO2 word_call/default notification SFX."/>
        </Grid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Width="96" Content="OK" Style="{StaticResource ModernButton}" Margin="0,0,8,0" Click="OkButton_Click"/>
            <Button Width="96" Content="Cancel" Style="{StaticResource ModernButton}" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</local:OceanyaWindowContentControl>
