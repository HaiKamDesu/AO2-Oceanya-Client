<local:OceanyaWindowContentControl x:Class="OceanyaClient.FavoriteServerEditorDialog"
                                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                   xmlns:local="clr-namespace:OceanyaClient">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Server Name" Margin="0,0,0,4" FontWeight="SemiBold"/>
            <TextBox x:Name="ServerNameTextBox" Height="30" VerticalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Server Endpoint (ws:// or wss://)" Margin="0,0,0,4" FontWeight="SemiBold"/>
            <TextBox x:Name="ServerEndpointTextBox" Height="30" VerticalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Description" Margin="0,0,0,4" FontWeight="SemiBold"/>
            <TextBox x:Name="ServerDescriptionTextBox"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Width="100" Height="30" Content="Cancel" Click="CancelButton_Click"/>
            <Button x:Name="ActionButton" Width="120" Height="30" Margin="8,0,0,0" Content="Save" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</local:OceanyaWindowContentControl>
