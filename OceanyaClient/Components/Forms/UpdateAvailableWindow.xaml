<local:OceanyaWindowContentControl x:Class="OceanyaClient.UpdateAvailableWindow"
                                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                   xmlns:local="clr-namespace:OceanyaClient"
                                   Height="520"
                                   Width="720"
                                   MinHeight="420"
                                   MinWidth="620">
    <local:OceanyaWindowContentControl.Resources>
        <Style x:Key="ModernButton" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#FF262626"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,5"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="130"/>
            <Setter Property="Margin" Value="6,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF3D3D3D"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF171717"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </local:OceanyaWindowContentControl.Resources>

    <Grid Margin="18,12,18,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock"
                   Grid.Row="0"
                   Foreground="White"
                   FontSize="18"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"/>

        <TextBlock x:Name="SubtitleTextBlock"
                   Grid.Row="1"
                   Margin="0,6,0,10"
                   Foreground="#FFBDBDBD"
                   FontSize="12"
                   TextWrapping="Wrap"/>

        <Border Grid.Row="2"
                Background="#FF181818"
                BorderBrush="#FF383838"
                BorderThickness="1"
                CornerRadius="3">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="12">
                <TextBlock x:Name="ReleaseNotesTextBlock"
                           AutomationProperties.AutomationId="UpdateAvailable.ReleaseNotes"
                           Foreground="#FFE8E8E8"
                           FontFamily="Consolas"
                           FontSize="12"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,14,0,0">
            <Button x:Name="SkipButton"
                    AutomationProperties.AutomationId="UpdateAvailable.Skip"
                    Content="Skip this version"
                    Style="{StaticResource ModernButton}"
                    Click="SkipButton_Click"/>
            <Button x:Name="UpdateButton"
                    AutomationProperties.AutomationId="UpdateAvailable.Update"
                    Content="Update"
                    Style="{StaticResource ModernButton}"
                    IsDefault="True"
                    Click="UpdateButton_Click"/>
        </StackPanel>
    </Grid>
</local:OceanyaWindowContentControl>
